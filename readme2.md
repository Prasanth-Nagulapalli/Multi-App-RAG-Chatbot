python -m pip install -U pip
python -m pip install python-dotenv langchain langchain-community langchain-text-splitters chromadb sentence-transformers

how to run if not first built
python -m src.ingest

and run 
python -m src.chat_cli
